# Deliver-order HTTP endpoint. Uses 8082 so it does not collide with create-order (8081).
quarkus.http.port=8082
# Deliver-order gRPC endpoint. Uses 9102 so it does not collide with create-order (9101).
quarkus.grpc.server.port=9102
quarkus.log.category."org.pipelineframework".level=INFO
